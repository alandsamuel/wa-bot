{
  "help": {
    "header": "💡 Available Commands:",
    "footer": "\n• cancel - Cancel pending input",
    "expenseExample": "\n💰 Add Expense: \"makan nasi padang 20000\""
  },
  "categories": {
    "expenses": {
      "name": "📊 Expenses",
      "commands": {
        "!list": { "description": "List monthly expenses", "enabled": true },
        "!today": { "description": "Show today's expenses", "enabled": true },
        "!recent": { "description": "Show last 10 expenses", "enabled": true },
        "!top": { "description": "Top spending categories", "enabled": true },
        "!summarize": { "description": "Monthly summary by category", "enabled": true },
        "!search <term>": { "description": "Search expenses by description", "enabled": true }
      }
    },
    "budget": {
      "name": "💰 Budget",
      "commands": {
        "!budget": { "description": "Show budget status", "enabled": true },
        "!budget <amount>": { "description": "Set monthly budget (e.g. 5m)", "enabled": true }
      }
    },
    "preorder": {
      "name": "📦 Pre-Orders",
      "commands": {
        "!po": { "description": "Add new pre-order", "enabled": true },
        "!po list": { "description": "List all pre-orders", "enabled": true }
      }
    },
    "wishlist": {
      "name": "🛍️ Wishlist",
      "commands": {
        "!wishlist": { "description": "Add wishlist item", "enabled": true },
        "!wishlist list": { "description": "List all wishlist items", "enabled": true }
      }
    },
    "other": {
      "name": "🔗 Other",
      "commands": {
        "!notionlink": { "description": "Get Notion database link", "enabled": true },
        "Send receipt image": { "description": "Process receipt with Veryfi", "enabled": true }
      }
    }
  }
}
